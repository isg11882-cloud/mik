{
  "name": "mik-worker",
  "version": "1.0.0",
  "description": "MIK (MICE Insight Korea) — Cloudflare Worker Backend",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute mik-db --file=./schema.sql"
  },
  "devDependencies": {
    "wrangler": "^4.90.1"
  }
}
